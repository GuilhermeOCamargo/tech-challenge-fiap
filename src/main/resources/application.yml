spring:
  config:
    activate:
      on-profile: prd
  datasource:
    url: jdbc:mariadb://tech_challenge_db:3306/tech_challenge
    driver-class-name: org.mariadb.jdbc.Driver
    username: application_user
    password: SENHA123
  jpa:
    database-platform: org.hibernate.dialect.MariaDB103Dialect
    hibernate:
      ddl-auto: update

server:
  servlet:
    context-path: /tech-challenge
  port: 8080

---

spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mariadb://localhost:3306/tech_challenge
    driver-class-name: org.mariadb.jdbc.Driver
    username: application_user
    password: SENHA123
  jpa:
    database-platform: org.hibernate.dialect.MariaDB103Dialect
    hibernate:
      ddl-auto: update

server:
  servlet:
    context-path: /tech-challenge
  port: 8080

---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:h2:mem:localdb
    driver-class-name: org.h2.Driver
    username: test
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

server:
  servlet:
    context-path: /tech-challenge
  port: 8080